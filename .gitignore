# Gradle
.gradle/
build/
*/build/

# Android / IDE
local.properties
.idea/
*.iml
captures/
.cxx/

# Secrets — never commit these
keystore.properties
*.jks
*.keystore
app/google-services.json
play-service-account.json

# ...except the shared DEBUG key (not secret) so debug builds share a signature
!app/debug.keystore

# OS
.DS_Store
